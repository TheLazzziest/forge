pre-push:
  commands:
    validate-registry:
      # pre defined commands hangs lefthook
      run: "bunx ocx build . --out dist"
      glob: "registry.jsonc"

commit-msg:
  commands:
    commitlint:
      run: "bunx commitlint --edit {1}"
